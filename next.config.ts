/** @type {import('next').NextConfig} */
const nextConfig = {
  // Agregá la IP de tu celular acá
  allowedDevOrigins: ['192.168.1.43', 'localhost'],
  allowedDevOrigins: ['192.168.100.36', 'localhost'],
};

export default nextConfig;
